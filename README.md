# Hex mapping

A simple python project to convert a PNG image to a SVG file composed of hexagons.

## Installation

Install the dependencies using poetry:

```bash
poetry install
```

## Utilisation 

Put your image in the image/png folder. 

Use the OOP.py script to générate your svg file (in the main section). 
